We will implement in this code base a structure that comes from the ../Figures_BEACON_paper .  There we set the paths to get the 66 features. jt_features and extra_jt_features (:end-5:end,:). For both cohorts, the SCF and the HNG. And for now we will take the data only from SC and HN. The idea is that we will try to gather the data for figure 2. you see it? follow along the already generated figure 2 in the ../Figures_BEACON_paper/figures folder. now, let's only extract the data, convert them to the csv we need and all, from the main scriot: run_linear_v1.py You see? so we need to run the tun_linear_v1 script for each condition in each cohort (cohort being: SFC or HNG; conditions for cohort 1: S and F; and for cohort 2, H and N). You see it? for this we will start creating a new directory called: Analysis_MA. there you can createa all the scripts you need being matlab first to extract the cohort and conditions data, then to convert them to csv, normalizing it as it is normalized now (check the DATA_MA folder for normalization code).. And then a python
